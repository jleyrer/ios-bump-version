name: "iOS Bump Version"
description: "update Xcode Project version"
inputs:
  version:
    description: "new version string"
    required: false
outputs:
  version:
    description: "new version string"
runs:
  using: "node16"
  main: "dist/index.js"
branding:
  icon: "arrow-up"
  color: "black"
